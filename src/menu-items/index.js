// project import
import configuration from './configuration';
import DashboardMenu from './dashboard';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [DashboardMenu, configuration]
};

export default menuItems;
